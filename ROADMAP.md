# Roadmap

- First public npm + GitHub Release with provenance
- Homebrew tap automation (`UriBer/homebrew-tap`)
- Scoop bucket when Windows CI is green
- Optional native Windows launcher (explicit Git Bash/WSL bridge)
- Richer doctor issue codes JSON schema
- Prompt themes / length controls for long project IDs
