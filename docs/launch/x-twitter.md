# X / short post

Stop deploying to the wrong GCP project.

gcloud configs ≠ Application Default Credentials.
gcpctx switches account + project + ADC + quota as one context (venv-style prompt + assert for agents).

npm i -g gcpctx
https://github.com/UriBer/gcpctx
