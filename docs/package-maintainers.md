# Package maintainers

- npm: trusted publishing / OIDC preferred; `NPM_PUBLISH` repo variable gates workflow
- Homebrew: publish formula to `UriBer/homebrew-tap` with release SHA256
- Scoop: publish bucket with zip hash
- WinGet: blocked until native story is honest — see packaging/winget/README.md
